FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY rag/ ./rag/
COPY rag_server.py .
ENV USE_TF=0 USE_TORCH=1
EXPOSE 8100
CMD ["python", "rag_server.py", "--rebuild"]
